<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="NOAppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MaterialAlertDialog.App" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.App.MediumComponent</item>
    </style>
    <style name="ShapeAppearance.App.MediumComponent" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppThemeLightBasic0" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_green</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_green</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_green</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_green</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic1" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_pink</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_pink</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_pink</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_pink</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic2" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_pink1</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_pink1</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_pink1</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_pink1</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic3" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_pink2</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_pink2</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_pink2</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_pink2</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic4" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_blue</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_blue</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_blue</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_blue</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic5" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_red</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_red</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_red</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_red</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic6" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_pink3</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_pink3</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_pink3</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_pink3</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic7" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_green1</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_green1</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_green1</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_green1</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic8" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/colorPrimary</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/colorPrimary</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>
    <style name="AppThemeLightBasic9" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <item name="colorSwitchThumbNormal">@color/dark</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/dark</item>

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/black1</item>

        <!-- Customize your theme here. -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/nice_yellow</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/nice_yellow</item>
        <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/nice_yellow</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/nice_yellow</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/colorPrimaryDark</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/white</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>

    <style name="AppThemeDarkBasic" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--<item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->

        <!--override text color here-->
        <item name="android:textColorPrimary">@color/white</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/nice_green</item>

        <!--the color of app bar, button,...-->
        <item name="accent_color">@color/black</item>
        <!--the color of text view, text box-->
        <item name="text_color">@color/white70</item>
        <!--the opposite color if text-->
        <item name="them_color_opp">@color/white70</item>
        <!--button customize color if possible-->
        <item name="background_customize">@color/bg_customize1</item>
        <!--override default background color of theme-->
        <item name="background_color">@color/black0</item>

        <!-- Assign dialog style here. -->
        <item name="alertDialogTheme">@style/MyAlertDialogStyle</item>
        <item name="alertDialogStyle">@style/MyAlertDialogStyle</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">22sp</item>

    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />



<!--    <style name="AppThemeForNowPlaying" parent="Theme.AppCompat.NoActionBar">-->
<!--        <item name="android:statusBarColor">@android:color/transparent</item>-->
<!--        <item name="android:navigationBarColor">@android:color/transparent</item>-->
<!--        <item name="android:windowTranslucentStatus">true</item>-->
<!--        <item name="android:windowTranslucentNavigation">true</item>-->

<!--        <item name="colorSwitchThumbNormal">@color/dark</item>-->
<!--        &lt;!&ndash; Inactive track color(30% transparency) &ndash;&gt;-->
<!--        <item name="android:colorForeground">@color/dark</item>-->

<!--        &lt;!&ndash;override text color here&ndash;&gt;-->
<!--        <item name="android:textColorPrimary">@color/black1</item>-->

<!--        &lt;!&ndash; Customize your theme here. &ndash;&gt;-->
<!--        &lt;!&ndash; colorPrimary is used for the default action bar background &ndash;&gt;-->
<!--        <item name="colorPrimary">@color/nice_green</item>-->
<!--        &lt;!&ndash; colorPrimaryDark is used for the status bar &ndash;&gt;-->
<!--        <item name="colorPrimaryDark">@color/nice_green</item>-->
<!--        &lt;!&ndash; colorAccent is used as the default value for colorControlActivated,-->
<!--         which is used to tint widgets &ndash;&gt;-->
<!--        <item name="colorAccent">@color/nice_green</item>-->

<!--        &lt;!&ndash;the color of app bar, button,...&ndash;&gt;-->
<!--        <item name="accent_color">@color/nice_green</item>-->
<!--        &lt;!&ndash;the color of text view, text box&ndash;&gt;-->
<!--        <item name="text_color">@color/colorPrimaryDark</item>-->
<!--        &lt;!&ndash;the opposite color if text&ndash;&gt;-->
<!--        <item name="them_color_opp">@color/white</item>-->
<!--        &lt;!&ndash;button customize color if possible&ndash;&gt;-->
<!--        <item name="background_customize">@color/bg_customize</item>-->
<!--        &lt;!&ndash;override default background color of theme&ndash;&gt;-->
<!--        <item name="background_color">@color/white</item>-->
<!--    </style>-->

    <!-- Dialog -->
    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/white70</item>
<!--        <item name="buttonBarPositiveButtonStyle">@style/MyAlertDialogButtonStyle</item>-->
<!--        <item name="buttonBarNegativeButtonStyle">@style/MyAlertDialogButtonStyle</item>-->
<!--        <item name="buttonBarNeutralButtonStyle">@style/MyAlertDialogButtonStyle</item>-->
    </style>

<!--    <style name="MyAlertDialogButtonStyle" parent="@style/Widget.AppCompat.Button.ButtonBar.AlertDialog">-->
<!--        <item name="android:textColor">@color/colorAccent</item>-->
<!--        <item name="android:textSize">@dimen/text_size_medium</item>-->
<!--    </style>-->
    <style name="MyDialogFadeAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.WhiteAccent">
        <item name="colorAccent">@color/white</item> <!-- Whatever color you want-->
    </style>

    <style name="RoundShapeAppearance" parent="ShapeAppearance.MaterialComponents.MediumComponent">

        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>


    <style name="AlertDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <!--        <item name="buttonBarPositiveButtonStyle">@style/Alert.Button.Positive</item>-->
        <!--        <item name="buttonBarNegativeButtonStyle">@style/Alert.Button.Neutral</item>-->
        <item name="shapeAppearanceOverlay">@style/RoundShapeAppearance</item>
    </style>

</resources>
